class Solution {
    public boolean isPalindrome(int x) {
        if(x<0)
        return false;
        int t=x;
        long ans=0;
        while(t!=0)
        {
            int d=t%10;
            ans=ans*10 +d;
            t=t/10;
        }
        return x==ans;

    }
}
